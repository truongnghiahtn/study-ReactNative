export class Place {
  constructor(title, imageUri, location,id) {
    this.title = title;
    this.imageUri = imageUri;
    this.address = "71 ven kênh";
    this.location = location; // { lat: 0.141241, lng: 127.121 }
    this.id = id
  }
}
